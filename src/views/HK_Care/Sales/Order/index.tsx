import { PageWrapper } from 'components/common';
import TableData from './TableData';

const Order = () => {
  return (
    <PageWrapper title="Hóa đơn bán hàng">
      <TableData />
    </PageWrapper>
  );
};

export default Order;
