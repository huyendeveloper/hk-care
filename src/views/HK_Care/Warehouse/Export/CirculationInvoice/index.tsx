import { PageWrapper } from 'components/common';
import TableData from './TableData';

const Receipt = () => {
  return (
    <PageWrapper title="Xuất luân chuyển">
      <TableData />
    </PageWrapper>
  );
};

export default Receipt;
