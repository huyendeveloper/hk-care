const baseURL = process.env.REACT_APP_BASE_URL + '/api/app';

const connectURL = process.env.REACT_APP_BASE_URL;

const version = process.env.REACT_APP_VERSION;

export { baseURL, connectURL, version };
